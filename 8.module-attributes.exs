defmodule MyServer do
  @moduledoc """
  This is just an example of a module that could be a server
  Although this is just a simple example that doesn't do much
  """

  @doc """
  This would serve as a docstring for the following method
  example\\\\0 here does nothing, and is a no-op method
  """
  def example() do
    # noop
  end

end

